# NEXUSFLOW-ENGINE — never commit real secrets.
# Server / Render matching worker
DATABASE_URL=
NEXUSFLOW_SERVICE_ROLE_KEY=
# Optional Stripe for enterprise billing of API seats
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Frontend (Vercel) public
VITE_API_BASE=
