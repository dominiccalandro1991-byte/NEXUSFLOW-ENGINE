# Testing

```
npm test
npm run bench
```

FIFO, partial fills, cancel unlink, best bid/ask cache, market remainder, duplicate/size/concurrent mutation, ledger balance, adversarial mid-level cancel, open-order cap, rate limit, sequencer, ownership.
